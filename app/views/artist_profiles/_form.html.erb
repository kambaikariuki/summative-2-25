<%= form_with(model: artist_profile) do |form| %>
  <% if artist_profile.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(artist_profile.errors.count, "error") %> prohibited this artist_profile from being saved:</h2>

      <ul>
        <% artist_profile.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.textarea :description %>
  </div>

  <div>
    <%= form.label :artist_id, style: "display: block" %>
    <%= form.number_field :artist_id %>
  </div>

  <div>
    <%= form.label :photo_id, style: "display: block" %>
    <%= form.number_field :photo_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
